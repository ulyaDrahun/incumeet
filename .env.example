# Lovable Cloud (Supabase) — auto-managed by Lovable; safe publishable values only.
# Do NOT put service_role keys or any private secrets here. Private secrets belong
# in Lovable Cloud → Backend Functions → Secrets (never in .env).

VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-publishable-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
